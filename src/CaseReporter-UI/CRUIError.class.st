"
I am an error coming from the UI part of CaseReporter.

So, most probably, it is because of user actions I have been signaled.

For example, if user cancel login, one of my subclass is signaled.
"
Class {
	#name : #CRUIError,
	#superclass : #CRError,
	#category : #'CaseReporter-UI-Errors'
}
